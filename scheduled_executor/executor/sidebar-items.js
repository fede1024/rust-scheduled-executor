initSidebarItems({"struct":[["CoreExecutor","A `CoreExecutor` is the most simple executor provided. It runs a single thread, which is responsible for both scheduling the closure (registering the timer for the wakeup), and the actual execution of the closure. The executor will stop once dropped. The `CoreExecutor` can be cloned to generate a new reference to the same underlying executor."],["ThreadPoolExecutor",""]]});